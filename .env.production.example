# Production Environment Configuration
# IMPORTANT: Never commit actual secrets to version control

# Database Configuration (use AWS Secrets Manager in production)
# Set USE_AWS_SECRETS=true to use AWS Secrets Manager
USE_AWS_SECRETS=true
AWS_REGION=us-east-1

# If not using AWS Secrets Manager, use encrypted environment variables
# Generate these using the secrets management system
DB_HOST_ENCRYPTED=
DB_PORT_ENCRYPTED=
DB_NAME_ENCRYPTED=
DB_USER_ENCRYPTED=
DB_PASSWORD_ENCRYPTED=

# API Configuration
API_HOST=0.0.0.0
API_PORT=8001

# Docker Configuration
DOCKER_HOST=unix:///var/run/docker.sock

# Logging Configuration
LOG_LEVEL=INFO

# Security (use strong, unique keys in production)
SECRET_KEY_ENCRYPTED=
JWT_SECRET_KEY_ENCRYPTED=

# CORS Origins (restrict to your actual domains)
CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# File Paths (use secure, isolated directories)
DUMP_BASE_PATH=/app/secure/dumps
RESTORE_BASE_PATH=/app/secure/dumps

# Redis Configuration (for rate limiting)
REDIS_URL=redis://localhost:6379

# AWS Secrets Manager secret names (if using AWS)
AWS_SECRET_DATABASE=prod/database/credentials
AWS_SECRET_JWT=prod/api/jwt-key
AWS_SECRET_API_KEY=prod/api/key